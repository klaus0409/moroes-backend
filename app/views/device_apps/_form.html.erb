<%= bootstrap_form_for([device, device_app]) do |form| %>

    <%= form.hidden_field :device_id %>

    <%= form.select :app_id, App.all.map{|a| [a.full_name, a.id]} %>

    <%= form.text_area :note, id: :device_app_note %>

    <%= form.submit %>
<% end %>
